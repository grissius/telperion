schema: src/schema/main.graphql
generates:
  ./src/resolvers-types.ts:
    plugins:
      - typescript
      - typescript-resolvers
# todo context type https://github.com/dotansimha/graphql-code-generator/issues/1090